# TPV (Terminal Punto de Venta) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Construir el TPV completo: mapa de mesas con Realtime, vista de comanda con modificadores, y pantalla de cobro con múltiples métodos de pago.

**Architecture:** Server Components cargan los datos iniciales y los pasan a Client Components. Todos los cambios van a través de Server Actions en `app/actions/tpv.ts`. El mapa de mesas se actualiza en tiempo real vía Supabase Realtime en el Client Component `TableMap`. Estado gestionado localmente con `useState` + actualizaciones optimistas.

**Tech Stack:** Next.js 16.2.6 · React 19 · TypeScript 5 strict · Tailwind v4 · @supabase/ssr v0.10.3

> **Verificación por tarea:** `npx tsc --noEmit`. Verificación final: `npm run build`.

---

## File Map

| Fichero | Estado | Responsabilidad |
|---------|--------|----------------|
| `lib/supabase/client.ts` | Crear | Cliente Supabase para el browser (Realtime) |
| `app/actions/tpv.ts` | Crear | Todos los Server Actions del TPV + tipos exportados |
| `app/tpv/page.tsx` | Crear | SC: carga zonas+mesas, renderiza TableMap |
| `components/tpv/TableCard.tsx` | Crear | CC: tarjeta visual de una mesa |
| `components/tpv/TableMap.tsx` | Crear | CC: mapa de mesas con Realtime subscription |
| `app/tpv/comanda/[orderId]/page.tsx` | Crear | SC: carga orden+productos, renderiza OrderView |
| `components/tpv/OrderView.tsx` | Crear | CC: layout split 60/40, gestiona estado de items |
| `components/tpv/ProductsPanel.tsx` | Crear | CC: tabs categorías, buscador, grid de productos |
| `components/tpv/OrderPanel.tsx` | Crear | CC: lista de líneas, totales, botón Cobrar |
| `components/tpv/ModifierModal.tsx` | Crear | CC: modal de selección de modificadores |
| `app/tpv/cobro/[orderId]/page.tsx` | Crear | SC: carga orden para cobro |
| `components/tpv/PaymentForm.tsx` | Crear | CC: selector método pago + cálculo cambio |
| `app/dashboard/page.tsx` | Modificar | Añadir enlace "Ir al TPV" |

---

## Task 1: Browser Supabase client + Server Actions

**Files:**
- Create: `lib/supabase/client.ts`
- Create: `app/actions/tpv.ts`

- [ ] **Step 1: Crear `lib/supabase/client.ts`**

```typescript
import { createBrowserClient } from '@supabase/ssr'

export function createClient() {
  return createBrowserClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY!
  )
}
```

- [ ] **Step 2: Crear `app/actions/tpv.ts`**

```typescript
'use server'

import { redirect } from 'next/navigation'
import { createClient } from '@/lib/supabase/server'

// ─── Types ────────────────────────────────────────────────────────────────────

export type TableStatus = 'free' | 'occupied' | 'reserved' | 'billing'
export type OrderItemStatus = 'pending' | 'preparing' | 'ready' | 'served' | 'cancelled'
export type PaymentMethod = 'cash' | 'card' | 'bizum' | 'mixed'

export type ModifierOption = {
  id: string
  name: string
  price_adjustment: number
}

export type ModifierGroup = {
  id: string
  name: string
  is_required: boolean
  min_selections: number
  max_selections: number
  options: ModifierOption[]
}

export type ProductWithModifiers = {
  id: string
  name: string
  price: number
  tax_rate: number
  is_available: boolean
  category_id: string
  modifierGroups: ModifierGroup[]
}

export type Category = {
  id: string
  name: string
}

export type TableWithOrder = {
  id: string
  name: string
  capacity: number
  status: TableStatus
  openOrder?: { id: string; total: number; opened_at: string }
}

export type ZoneWithTables = {
  id: string
  name: string
  color: string
  tables: TableWithOrder[]
}

export type SelectedModifier = {
  option_id: string
  name: string
  price_adjustment: number
}

export type OrderItem = {
  id: string
  product_name: string
  product_price: number
  tax_rate: number
  quantity: number
  unit_price: number
  total_price: number
  modifiers: SelectedModifier[]
  notes: string | null
  status: OrderItemStatus
}

export type OrderWithItems = {
  id: string
  order_number: number
  status: string
  opened_at: string
  total: number
  table: { id: string; name: string }
  items: OrderItem[]
}

export type ProcessPaymentParams =
  | { method: 'cash'; cashAmount: number; changeGiven: number }
  | { method: 'card'; amount: number }
  | { method: 'bizum'; amount: number }
  | { method: 'mixed'; cashAmount: number; cardAmount: number }

// ─── Helper ───────────────────────────────────────────────────────────────────

async function getRestaurantId(
  supabase: Awaited<ReturnType<typeof createClient>>,
  userId: string
): Promise<string | null> {
  const { data } = await supabase
    .from('users')
    .select('restaurant_id')
    .eq('id', userId)
    .single()
  return data?.restaurant_id ?? null
}

// ─── Actions ──────────────────────────────────────────────────────────────────

export async function getZonesWithTables(): Promise<ZoneWithTables[]> {
  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) redirect('/login')

  const restaurantId = await getRestaurantId(supabase, user.id)
  if (!restaurantId) redirect('/login')

  const { data: zones } = await supabase
    .from('zones')
    .select('id, name, color')
    .eq('restaurant_id', restaurantId)
    .eq('is_active', true)
    .is('deleted_at', null)
    .order('position')

  if (!zones || zones.length === 0) return []

  const { data: tables } = await supabase
    .from('tables')
    .select('id, zone_id, name, capacity, status')
    .eq('restaurant_id', restaurantId)
    .eq('is_active', true)
    .is('deleted_at', null)
    .order('position')

  const tableIds = (tables ?? []).map(t => t.id)

  const { data: openOrders } = tableIds.length > 0
    ? await supabase
        .from('orders')
        .select('id, table_id, total, opened_at')
        .in('table_id', tableIds)
        .eq('status', 'open')
        .is('deleted_at', null)
    : { data: [] as { id: string; table_id: string; total: number; opened_at: string }[] }

  return zones.map(zone => ({
    id: zone.id,
    name: zone.name,
    color: zone.color,
    tables: (tables ?? [])
      .filter(t => t.zone_id === zone.id)
      .map(t => {
        const order = (openOrders ?? []).find(o => o.table_id === t.id)
        return {
          id: t.id,
          name: t.name,
          capacity: t.capacity,
          status: t.status as TableStatus,
          openOrder: order
            ? { id: order.id, total: Number(order.total), opened_at: order.opened_at }
            : undefined,
        }
      }),
  }))
}

export async function getOpenOrder(tableId: string): Promise<{ orderId: string } | null> {
  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) redirect('/login')

  const { data } = await supabase
    .from('orders')
    .select('id')
    .eq('table_id', tableId)
    .eq('status', 'open')
    .is('deleted_at', null)
    .maybeSingle()

  return data ? { orderId: data.id } : null
}

export async function createOrder(tableId: string): Promise<{ orderId: string } | { error: string }> {
  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) redirect('/login')

  const restaurantId = await getRestaurantId(supabase, user.id)
  if (!restaurantId) redirect('/login')

  const today = new Date().toISOString().split('T')[0]

  const { data: order, error } = await supabase
    .from('orders')
    .insert({
      restaurant_id: restaurantId,
      table_id: tableId,
      status: 'open',
      type: 'dine_in',
      opened_by: user.id,
      opened_at: new Date().toISOString(),
      order_date: today,
    })
    .select('id')
    .single()

  if (error || !order) return { error: 'No se pudo crear la comanda' }

  await supabase.from('tables').update({ status: 'occupied' }).eq('id', tableId)

  return { orderId: order.id }
}

export async function getOrderWithItems(orderId: string): Promise<OrderWithItems | null> {
  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) redirect('/login')

  const restaurantId = await getRestaurantId(supabase, user.id)
  if (!restaurantId) redirect('/login')

  const { data: order } = await supabase
    .from('orders')
    .select('id, order_number, status, opened_at, total, table_id')
    .eq('id', orderId)
    .eq('restaurant_id', restaurantId)
    .maybeSingle()

  if (!order) return null

  const { data: table } = await supabase
    .from('tables')
    .select('id, name')
    .eq('id', order.table_id)
    .single()

  const { data: items } = await supabase
    .from('order_items')
    .select('id, product_name, product_price, tax_rate, quantity, unit_price, total_price, modifiers, notes, status')
    .eq('order_id', orderId)
    .neq('status', 'cancelled')
    .order('created_at')

  return {
    id: order.id,
    order_number: order.order_number,
    status: order.status,
    opened_at: order.opened_at,
    total: Number(order.total),
    table: { id: table!.id, name: table!.name },
    items: (items ?? []).map(item => ({
      id: item.id,
      product_name: item.product_name,
      product_price: Number(item.product_price),
      tax_rate: Number(item.tax_rate),
      quantity: item.quantity,
      unit_price: Number(item.unit_price),
      total_price: Number(item.total_price),
      modifiers: (item.modifiers as SelectedModifier[]) ?? [],
      notes: item.notes,
      status: item.status as OrderItemStatus,
    })),
  }
}

export async function getMenuData(): Promise<{ categories: Category[]; products: ProductWithModifiers[] }> {
  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) redirect('/login')

  const restaurantId = await getRestaurantId(supabase, user.id)
  if (!restaurantId) redirect('/login')

  const { data: categories } = await supabase
    .from('categories')
    .select('id, name')
    .eq('restaurant_id', restaurantId)
    .is('deleted_at', null)
    .order('position')

  const { data: products } = await supabase
    .from('products')
    .select('id, name, price, tax_rate, is_available, category_id')
    .eq('restaurant_id', restaurantId)
    .eq('is_visible', true)
    .is('deleted_at', null)
    .order('position')

  const productIds = (products ?? []).map(p => p.id)

  const { data: groups } = productIds.length > 0
    ? await supabase
        .from('product_modifier_groups')
        .select('id, product_id, name, is_required, min_selections, max_selections, position')
        .in('product_id', productIds)
        .is('deleted_at', null)
        .order('position')
    : { data: [] as { id: string; product_id: string; name: string; is_required: boolean; min_selections: number; max_selections: number; position: number }[] }

  const groupIds = (groups ?? []).map(g => g.id)

  const { data: options } = groupIds.length > 0
    ? await supabase
        .from('product_modifier_options')
        .select('id, group_id, name, price_adjustment, position')
        .in('group_id', groupIds)
        .eq('is_active', true)
        .is('deleted_at', null)
        .order('position')
    : { data: [] as { id: string; group_id: string; name: string; price_adjustment: number; position: number }[] }

  return {
    categories: categories ?? [],
    products: (products ?? []).map(p => ({
      id: p.id,
      name: p.name,
      price: Number(p.price),
      tax_rate: Number(p.tax_rate),
      is_available: p.is_available,
      category_id: p.category_id,
      modifierGroups: (groups ?? [])
        .filter(g => g.product_id === p.id)
        .map(g => ({
          id: g.id,
          name: g.name,
          is_required: g.is_required,
          min_selections: g.min_selections,
          max_selections: g.max_selections,
          options: (options ?? [])
            .filter(o => o.group_id === g.id)
            .map(o => ({ id: o.id, name: o.name, price_adjustment: Number(o.price_adjustment) })),
        })),
    })),
  }
}

export async function addOrderItem(
  orderId: string,
  productId: string,
  quantity: number,
  modifiers: SelectedModifier[],
  notes?: string
): Promise<{ itemId: string } | { error: string }> {
  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) redirect('/login')

  const restaurantId = await getRestaurantId(supabase, user.id)
  if (!restaurantId) redirect('/login')

  const { data: product } = await supabase
    .from('products')
    .select('name, price, tax_rate')
    .eq('id', productId)
    .single()

  if (!product) return { error: 'Producto no encontrado' }

  const basePrice = Number(product.price)
  const unitPrice = basePrice + modifiers.reduce((sum, m) => sum + m.price_adjustment, 0)
  const totalPrice = unitPrice * quantity

  const { data: item, error } = await supabase
    .from('order_items')
    .insert({
      restaurant_id: restaurantId,
      order_id: orderId,
      product_id: productId,
      product_name: product.name,
      product_price: basePrice,
      tax_rate: Number(product.tax_rate),
      quantity,
      unit_price: unitPrice,
      total_price: totalPrice,
      modifiers,
      notes: notes ?? null,
      status: 'pending',
    })
    .select('id')
    .single()

  if (error || !item) return { error: 'No se pudo añadir el producto' }
  return { itemId: item.id }
}

export async function updateOrderItemQuantity(
  itemId: string,
  quantity: number
): Promise<{ error?: string }> {
  if (quantity <= 0) return removeOrderItem(itemId)

  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) redirect('/login')

  const { data: item } = await supabase
    .from('order_items')
    .select('unit_price')
    .eq('id', itemId)
    .single()

  if (!item) return { error: 'Línea no encontrada' }

  const { error } = await supabase
    .from('order_items')
    .update({ quantity, total_price: Number(item.unit_price) * quantity })
    .eq('id', itemId)

  if (error) return { error: 'No se pudo actualizar la cantidad' }
  return {}
}

export async function removeOrderItem(itemId: string): Promise<{ error?: string }> {
  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) redirect('/login')

  const { error } = await supabase
    .from('order_items')
    .update({
      status: 'cancelled',
      cancelled_at: new Date().toISOString(),
      cancelled_by: user.id,
    })
    .eq('id', itemId)

  if (error) return { error: 'No se pudo eliminar la línea' }
  return {}
}

export async function cancelOrder(orderId: string): Promise<{ error?: string }> {
  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) redirect('/login')

  const { data: order } = await supabase
    .from('orders')
    .select('table_id')
    .eq('id', orderId)
    .single()

  if (!order) return { error: 'Comanda no encontrada' }

  await supabase
    .from('order_items')
    .update({ status: 'cancelled', cancelled_at: new Date().toISOString(), cancelled_by: user.id })
    .eq('order_id', orderId)
    .neq('status', 'cancelled')

  const { error } = await supabase
    .from('orders')
    .update({ status: 'cancelled', closed_by: user.id, closed_at: new Date().toISOString() })
    .eq('id', orderId)

  if (error) return { error: 'No se pudo cancelar la comanda' }

  await supabase.from('tables').update({ status: 'free' }).eq('id', order.table_id)
  return {}
}

export async function processPayment(
  orderId: string,
  params: ProcessPaymentParams
): Promise<{ ticketId: string } | { error: string }> {
  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) redirect('/login')

  const restaurantId = await getRestaurantId(supabase, user.id)
  if (!restaurantId) redirect('/login')

  const { data: order } = await supabase
    .from('orders')
    .select('id, table_id, subtotal, tax_amount, total')
    .eq('id', orderId)
    .eq('status', 'open')
    .maybeSingle()

  if (!order) return { error: 'Comanda no encontrada o ya cobrada' }

  const { data: restaurant } = await supabase
    .from('restaurants')
    .select('name, nif, address')
    .eq('id', restaurantId)
    .single()

  if (!restaurant) return { error: 'Error al cargar los datos del restaurante' }

  const { data: items } = await supabase
    .from('order_items')
    .select('tax_rate, total_price')
    .eq('order_id', orderId)
    .neq('status', 'cancelled')

  const taxMap = new Map<number, { base: number; amount: number }>()
  for (const item of items ?? []) {
    const rate = Number(item.tax_rate)
    const totalWithTax = Number(item.total_price)
    const base = totalWithTax / (1 + rate / 100)
    const taxAmount = totalWithTax - base
    const prev = taxMap.get(rate) ?? { base: 0, amount: 0 }
    taxMap.set(rate, { base: prev.base + base, amount: prev.amount + taxAmount })
  }
  const taxBreakdown = Array.from(taxMap.entries()).map(([rate, v]) => ({
    rate,
    base: Number(v.base.toFixed(2)),
    amount: Number(v.amount.toFixed(2)),
  }))

  const { data: seqNum, error: seqError } = await supabase
    .rpc('get_next_ticket_number', { p_restaurant_id: restaurantId })

  if (seqError || seqNum === null) return { error: 'Error al generar el número de ticket' }

  const total = Number(order.total)
  const ticketNumber = `A-${String(seqNum).padStart(8, '0')}`

  const { data: ticket, error: ticketError } = await supabase
    .from('tickets')
    .insert({
      restaurant_id: restaurantId,
      order_id: orderId,
      ticket_number: ticketNumber,
      series: 'A',
      sequential_number: seqNum,
      issuer_name: restaurant.name,
      issuer_nif: restaurant.nif,
      issuer_address: restaurant.address ?? '',
      issued_at: new Date().toISOString(),
      subtotal: Number(order.subtotal),
      tax_breakdown: taxBreakdown,
      tax_total: Number(order.tax_amount),
      total,
      payment_method: params.method,
    })
    .select('id')
    .single()

  if (ticketError || !ticket) return { error: 'Error al crear el ticket' }

  const now = new Date().toISOString()
  if (params.method === 'mixed') {
    await supabase.from('payments').insert([
      { restaurant_id: restaurantId, ticket_id: ticket.id, method: 'cash', amount: params.cashAmount, change_given: 0, processed_by: user.id, processed_at: now },
      { restaurant_id: restaurantId, ticket_id: ticket.id, method: 'card', amount: params.cardAmount, change_given: 0, processed_by: user.id, processed_at: now },
    ])
  } else if (params.method === 'cash') {
    await supabase.from('payments').insert({
      restaurant_id: restaurantId, ticket_id: ticket.id, method: 'cash',
      amount: total, change_given: params.changeGiven, processed_by: user.id, processed_at: now,
    })
  } else {
    await supabase.from('payments').insert({
      restaurant_id: restaurantId, ticket_id: ticket.id, method: params.method,
      amount: total, change_given: 0, processed_by: user.id, processed_at: now,
    })
  }

  await supabase.from('orders').update({ status: 'paid', closed_by: user.id, closed_at: now }).eq('id', orderId)
  await supabase.from('tables').update({ status: 'free' }).eq('id', order.table_id)

  return { ticketId: ticket.id }
}
```

- [ ] **Step 3: Verificar TypeScript**

```powershell
npx tsc --noEmit
```

Resultado esperado: sin errores.

- [ ] **Step 4: Commit**

```powershell
git add lib/supabase/client.ts app/actions/tpv.ts
git commit -m "feat: add TPV server actions and types"
```

---

## Task 2: Mapa de mesas

**Files:**
- Create: `app/tpv/page.tsx`
- Create: `components/tpv/TableCard.tsx`
- Create: `components/tpv/TableMap.tsx`

- [ ] **Step 1: Crear `app/tpv/page.tsx`**

```tsx
import { redirect } from 'next/navigation'
import { createClient } from '@/lib/supabase/server'
import { getZonesWithTables } from '@/app/actions/tpv'
import TableMap from '@/components/tpv/TableMap'

export default async function TPVPage() {
  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) redirect('/login')

  const { data: userData } = await supabase
    .from('users')
    .select('restaurant_id')
    .eq('id', user.id)
    .single()

  if (!userData?.restaurant_id) redirect('/login')

  const initialData = await getZonesWithTables()

  return (
    <TableMap
      initialData={initialData}
      restaurantId={userData.restaurant_id}
    />
  )
}
```

- [ ] **Step 2: Crear `components/tpv/TableCard.tsx`**

```tsx
'use client'

import type { TableWithOrder } from '@/app/actions/tpv'

interface Props {
  table: TableWithOrder
  onClick: () => void
}

function formatElapsed(openedAt: string): string {
  const minutes = Math.floor((Date.now() - new Date(openedAt).getTime()) / 60000)
  if (minutes < 60) return `${minutes} min`
  const h = Math.floor(minutes / 60)
  const m = minutes % 60
  return m > 0 ? `${h}h ${m}min` : `${h}h`
}

const STYLES: Record<string, { card: string; badge: string }> = {
  free:     { card: 'border-green-200',                badge: 'bg-green-50 text-green-700 border border-green-200' },
  occupied: { card: 'border-red-200 bg-red-50',        badge: 'bg-red-50 text-red-700 border border-red-200' },
  reserved: { card: 'border-yellow-200 bg-yellow-50',  badge: 'bg-yellow-50 text-yellow-700 border border-yellow-200' },
  billing:  { card: 'border-blue-200 bg-blue-50',      badge: 'bg-blue-50 text-blue-700 border border-blue-200' },
}

const LABELS: Record<string, string> = {
  free: 'Libre', occupied: 'Ocupada', reserved: 'Reservada', billing: 'Cobrando',
}

export default function TableCard({ table, onClick }: Props) {
  const s = STYLES[table.status] ?? STYLES.free
  const totalColor = table.status === 'billing' ? 'text-blue-700' : 'text-red-600'

  return (
    <button
      onClick={onClick}
      style={{ minHeight: '90px' }}
      className={`w-full bg-white border-[1.5px] rounded-[10px] p-3 text-left transition-all hover:shadow-md hover:-translate-y-0.5 flex flex-col justify-between ${s.card}`}
    >
      <div className="flex items-start justify-between gap-1">
        <span className="text-[17px] font-extrabold text-slate-900 leading-tight">{table.name}</span>
        <span className={`text-[10px] font-bold px-1.5 py-0.5 rounded uppercase tracking-wide flex-shrink-0 ${s.badge}`}>
          {LABELS[table.status]}
        </span>
      </div>
      <div className="text-xs text-slate-400 mt-1">{table.capacity} personas</div>
      <div className="mt-2">
        {table.openOrder ? (
          <>
            <div className="text-[11px] text-slate-400">{formatElapsed(table.openOrder.opened_at)}</div>
            <div className={`text-[15px] font-extrabold ${totalColor}`}>
              {Number(table.openOrder.total).toFixed(2)} €
            </div>
          </>
        ) : (
          <div className="text-[11px] text-slate-400">
            {table.status === 'reserved' ? 'Reservada' : 'Toca para abrir'}
          </div>
        )}
      </div>
    </button>
  )
}
```

- [ ] **Step 3: Crear `components/tpv/TableMap.tsx`**

```tsx
'use client'

import { useState, useEffect, useTransition } from 'react'
import { useRouter } from 'next/navigation'
import { createClient } from '@/lib/supabase/client'
import type { ZoneWithTables, TableWithOrder } from '@/app/actions/tpv'
import { getZonesWithTables, createOrder, getOpenOrder } from '@/app/actions/tpv'
import TableCard from './TableCard'

interface Props {
  initialData: ZoneWithTables[]
  restaurantId: string
}

export default function TableMap({ initialData, restaurantId }: Props) {
  const router = useRouter()
  const [zones, setZones] = useState<ZoneWithTables[]>(initialData)
  const [error, setError] = useState<string | null>(null)
  const [isPending, startTransition] = useTransition()

  // Realtime subscription
  useEffect(() => {
    const supabase = createClient()
    const channel = supabase
      .channel(`tpv:tables:${restaurantId}`)
      .on(
        'postgres_changes',
        { event: 'UPDATE', schema: 'public', table: 'tables', filter: `restaurant_id=eq.${restaurantId}` },
        (payload) => {
          const updated = payload.new as { id: string; status: string; [key: string]: unknown }
          setZones(prev =>
            prev.map(zone => ({
              ...zone,
              tables: zone.tables.map(t =>
                t.id === updated.id
                  ? { ...t, status: updated.status as TableWithOrder['status'] }
                  : t
              ),
            }))
          )
        }
      )
      .subscribe()

    return () => { supabase.removeChannel(channel) }
  }, [restaurantId])

  const handleRefresh = () => {
    startTransition(async () => {
      const data = await getZonesWithTables()
      setZones(data)
    })
  }

  const handleTableClick = (table: TableWithOrder) => {
    setError(null)
    startTransition(async () => {
      if (table.status === 'free' || table.status === 'reserved') {
        const result = await createOrder(table.id)
        if ('error' in result) { setError(result.error); return }
        router.push(`/tpv/comanda/${result.orderId}`)
      } else {
        const result = await getOpenOrder(table.id)
        if (!result) { setError('No se encontró la comanda de esta mesa'); return }
        router.push(`/tpv/comanda/${result.orderId}`)
      }
    })
  }

  const stats = zones.flatMap(z => z.tables).reduce(
    (acc, t) => { acc[t.status] = (acc[t.status] ?? 0) + 1; return acc },
    {} as Record<string, number>
  )

  return (
    <div className="min-h-screen bg-[#f4f6f9]">
      {/* Nav */}
      <nav className="bg-white border-b border-slate-200 px-5 h-[52px] flex items-center justify-between shadow-sm">
        <div className="flex items-center gap-2 text-[15px] font-bold text-slate-900">
          RP Restaurantes
          <span className="bg-blue-600 text-white text-[11px] font-bold px-2 py-0.5 rounded">TPV</span>
        </div>
        <div className="flex gap-2">
          <button
            onClick={handleRefresh}
            disabled={isPending}
            className="bg-slate-100 border border-slate-200 text-slate-600 text-[13px] font-medium px-3 py-1.5 rounded-md hover:bg-slate-200 disabled:opacity-50"
          >
            {isPending ? 'Cargando...' : 'Actualizar'}
          </button>
          <button
            onClick={() => router.push('/dashboard')}
            className="bg-slate-100 border border-slate-200 text-slate-600 text-[13px] font-medium px-3 py-1.5 rounded-md hover:bg-slate-200"
          >
            Dashboard
          </button>
        </div>
      </nav>

      <div className="p-6 max-w-5xl mx-auto">
        <h1 className="text-[22px] font-bold text-slate-900 mb-4">Mapa de mesas</h1>

        {/* Stats */}
        <div className="flex gap-3 mb-6 flex-wrap">
          {[
            { status: 'occupied', label: 'Ocupadas', color: '#ef4444' },
            { status: 'free',     label: 'Libres',   color: '#22c55e' },
            { status: 'reserved', label: 'Reservadas', color: '#eab308' },
            { status: 'billing',  label: 'Cobrando', color: '#3b82f6' },
          ].map(({ status, label, color }) => (
            (stats[status] ?? 0) > 0 && (
              <div key={status} className="bg-white border border-slate-200 rounded-[8px] px-4 py-2 flex items-center gap-2 shadow-sm">
                <div className="w-2 h-2 rounded-full" style={{ background: color }} />
                <span className="text-[18px] font-bold" style={{ color }}>{stats[status]}</span>
                <span className="text-[12px] text-slate-400 font-medium">{label}</span>
              </div>
            )
          ))}
        </div>

        {error && <p className="text-red-600 text-sm mb-4">{error}</p>}

        {/* Zones */}
        {zones.map(zone => (
          <div key={zone.id} className="mb-8">
            <div className="flex items-center gap-2 mb-3">
              <div className="w-[3px] h-4 rounded-sm" style={{ background: zone.color || '#94a3b8' }} />
              <span className="text-[12px] font-bold text-slate-500 uppercase tracking-widest">{zone.name}</span>
              <span className="text-[11px] text-slate-400 bg-slate-100 px-2 py-0.5 rounded-full">{zone.tables.length} mesas</span>
              <div className="flex-1 h-px bg-slate-200" />
            </div>
            <div className="grid gap-3" style={{ gridTemplateColumns: 'repeat(auto-fill, minmax(140px, 1fr))' }}>
              {zone.tables.map(table => (
                <TableCard key={table.id} table={table} onClick={() => handleTableClick(table)} />
              ))}
            </div>
          </div>
        ))}

        {zones.length === 0 && (
          <p className="text-slate-500 text-sm">No hay zonas configuradas.</p>
        )}
      </div>
    </div>
  )
}
```

- [ ] **Step 4: Verificar TypeScript**

```powershell
npx tsc --noEmit
```

- [ ] **Step 5: Commit**

```powershell
git add app/tpv/page.tsx components/tpv/TableCard.tsx components/tpv/TableMap.tsx
git commit -m "feat: add TPV table map with Realtime"
```

---

## Task 3: Vista de comanda

**Files:**
- Create: `app/tpv/comanda/[orderId]/page.tsx`
- Create: `components/tpv/OrderView.tsx`
- Create: `components/tpv/ProductsPanel.tsx`
- Create: `components/tpv/OrderPanel.tsx`

- [ ] **Step 1: Crear `app/tpv/comanda/[orderId]/page.tsx`**

```tsx
import { redirect } from 'next/navigation'
import { getOrderWithItems, getMenuData } from '@/app/actions/tpv'
import OrderView from '@/components/tpv/OrderView'

interface Props {
  params: Promise<{ orderId: string }>
}

export default async function ComandaPage({ params }: Props) {
  const { orderId } = await params
  const [orderData, menuData] = await Promise.all([
    getOrderWithItems(orderId),
    getMenuData(),
  ])

  if (!orderData || orderData.status === 'paid' || orderData.status === 'cancelled') {
    redirect('/tpv')
  }

  return (
    <OrderView
      order={orderData}
      categories={menuData.categories}
      products={menuData.products}
    />
  )
}
```

- [ ] **Step 2: Crear `components/tpv/OrderPanel.tsx`**

```tsx
'use client'

import { useTransition } from 'react'
import { useRouter } from 'next/navigation'
import type { OrderItem, OrderWithItems } from '@/app/actions/tpv'
import { updateOrderItemQuantity, removeOrderItem, cancelOrder } from '@/app/actions/tpv'

interface Props {
  order: OrderWithItems
  items: OrderItem[]
  onItemsChange: (updater: (prev: OrderItem[]) => OrderItem[]) => void
  onError: (msg: string) => void
}

function calcTotals(items: OrderItem[]) {
  const total = items.reduce((s, i) => s + i.total_price, 0)
  const taxMap = new Map<number, number>()
  for (const item of items) {
    const rate = item.tax_rate
    const tax = item.total_price - item.total_price / (1 + rate / 100)
    taxMap.set(rate, (taxMap.get(rate) ?? 0) + tax)
  }
  const subtotal = total - Array.from(taxMap.values()).reduce((s, v) => s + v, 0)
  return { subtotal, taxBreakdown: Array.from(taxMap.entries()), total }
}

export default function OrderPanel({ order, items, onItemsChange, onError }: Props) {
  const router = useRouter()
  const [isPending, startTransition] = useTransition()

  const { subtotal, taxBreakdown, total } = calcTotals(items)

  const handleQty = (itemId: string, newQty: number) => {
    onItemsChange(prev => newQty <= 0
      ? prev.map(i => i.id === itemId ? { ...i, status: 'cancelled' as const } : i).filter(i => i.status !== 'cancelled')
      : prev.map(i => i.id === itemId ? { ...i, quantity: newQty, total_price: i.unit_price * newQty } : i)
    )
    startTransition(async () => {
      const res = await updateOrderItemQuantity(itemId, newQty)
      if (res?.error) onError(res.error)
    })
  }

  const handleRemove = (itemId: string) => {
    onItemsChange(prev => prev.filter(i => i.id !== itemId))
    startTransition(async () => {
      const res = await removeOrderItem(itemId)
      if (res?.error) onError(res.error)
    })
  }

  const handleCancel = () => {
    startTransition(async () => {
      const res = await cancelOrder(order.id)
      if (res?.error) { onError(res.error); return }
      router.push('/tpv')
    })
  }

  const canCancel = !items.some(i => i.status === 'served' || i.status === 'ready')

  return (
    <div className="flex flex-col h-full bg-white border-l border-slate-100">
      {/* Header */}
      <div className="px-4 py-3 border-b border-slate-100 flex-shrink-0">
        <div className="text-[18px] font-extrabold text-slate-900">{order.table.name}</div>
        <div className="text-[12px] text-slate-400 mt-0.5">
          Comanda #{String(order.order_number).padStart(4, '0')}
        </div>
      </div>

      {/* Items — scrollable */}
      <div className="flex-1 overflow-y-auto">
        {items.length === 0 ? (
          <p className="text-slate-400 text-sm text-center mt-8">Sin productos</p>
        ) : (
          items.map(item => (
            <div key={item.id} className="px-4 py-3 border-b border-slate-50">
              <div className="flex items-start justify-between gap-2">
                <span className="text-[14px] font-semibold text-slate-900 flex-1 leading-snug">{item.product_name}</span>
                <span className="text-[14px] font-bold text-slate-900 whitespace-nowrap">
                  {item.total_price.toFixed(2)} €
                </span>
              </div>
              {item.modifiers.length > 0 && (
                <p className="text-[11px] text-slate-500 mt-0.5">
                  {item.modifiers.map(m => m.name).join(' · ')}
                </p>
              )}
              <div className="flex items-center gap-2 mt-2">
                <button
                  onClick={() => handleQty(item.id, item.quantity - 1)}
                  disabled={isPending}
                  className="w-7 h-7 border border-slate-200 bg-slate-50 rounded-[6px] text-[16px] font-bold flex items-center justify-center text-slate-500 hover:bg-slate-100 disabled:opacity-40"
                >−</button>
                <span className="text-[15px] font-bold text-slate-900 w-5 text-center">{item.quantity}</span>
                <button
                  onClick={() => handleQty(item.id, item.quantity + 1)}
                  disabled={isPending}
                  className="w-7 h-7 border border-slate-200 bg-slate-50 rounded-[6px] text-[16px] font-bold flex items-center justify-center text-slate-500 hover:bg-slate-100 disabled:opacity-40"
                >+</button>
                <button
                  onClick={() => handleRemove(item.id)}
                  disabled={isPending}
                  className="ml-auto w-7 h-7 border border-red-100 bg-white rounded-[6px] text-red-400 hover:text-red-600 flex items-center justify-center text-[13px] disabled:opacity-40"
                >✕</button>
              </div>
            </div>
          ))
        )}
      </div>

      {/* Totals + Actions — fixed bottom */}
      <div className="flex-shrink-0 border-t border-slate-100">
        <div className="px-4 py-3 space-y-1">
          <div className="flex justify-between text-[13px] text-slate-500">
            <span>Subtotal</span><span>{subtotal.toFixed(2)} €</span>
          </div>
          {taxBreakdown.map(([rate, amount]) => (
            <div key={rate} className="flex justify-between text-[13px] text-slate-500">
              <span>IVA ({rate}%)</span><span>{amount.toFixed(2)} €</span>
            </div>
          ))}
          <div className="flex justify-between text-[20px] font-extrabold text-slate-900 pt-2 border-t border-slate-100 mt-2">
            <span>Total</span><span>{total.toFixed(2)} €</span>
          </div>
        </div>
        <div className="px-4 pb-4 space-y-2">
          <button
            onClick={() => router.push(`/tpv/cobro/${order.id}`)}
            disabled={items.length === 0}
            className="w-full bg-blue-600 hover:bg-blue-700 text-white font-bold text-[15px] py-4 rounded-[8px] disabled:opacity-40"
          >
            Cobrar — {total.toFixed(2)} €
          </button>
          {canCancel && (
            <button
              onClick={handleCancel}
              disabled={isPending}
              className="w-full bg-white border border-red-200 text-red-500 font-semibold text-[13px] py-2.5 rounded-[8px] hover:bg-red-50 disabled:opacity-40"
            >
              Cancelar comanda
            </button>
          )}
        </div>
      </div>
    </div>
  )
}
```

- [ ] **Step 3: Crear `components/tpv/ProductsPanel.tsx`**

```tsx
'use client'

import { useState, useTransition } from 'react'
import type { Category, ProductWithModifiers, SelectedModifier, OrderItem } from '@/app/actions/tpv'
import { addOrderItem } from '@/app/actions/tpv'
import ModifierModal from './ModifierModal'

interface Props {
  orderId: string
  categories: Category[]
  products: ProductWithModifiers[]
  onItemAdded: (item: OrderItem) => void
  onError: (msg: string) => void
}

export default function ProductsPanel({ orderId, categories, products, onItemAdded, onError }: Props) {
  const [activeCat, setActiveCat] = useState<string | null>(null)
  const [search, setSearch] = useState('')
  const [modalProduct, setModalProduct] = useState<ProductWithModifiers | null>(null)
  const [isPending, startTransition] = useTransition()

  const visible = products.filter(p => {
    if (activeCat && p.category_id !== activeCat) return false
    if (search.trim() && !p.name.toLowerCase().includes(search.toLowerCase())) return false
    return true
  })

  const handleAdd = (product: ProductWithModifiers, modifiers: SelectedModifier[]) => {
    setModalProduct(null)
    startTransition(async () => {
      const res = await addOrderItem(orderId, product.id, 1, modifiers)
      if ('error' in res) { onError(res.error); return }
      const unitPrice = product.price + modifiers.reduce((s, m) => s + m.price_adjustment, 0)
      onItemAdded({
        id: res.itemId,
        product_name: product.name,
        product_price: product.price,
        tax_rate: product.tax_rate,
        quantity: 1,
        unit_price: unitPrice,
        total_price: unitPrice,
        modifiers,
        notes: null,
        status: 'pending',
      })
    })
  }

  const handleProductClick = (product: ProductWithModifiers) => {
    if (!product.is_available) { onError('Este producto no está disponible'); return }
    if (product.modifierGroups.length > 0) {
      setModalProduct(product)
    } else {
      handleAdd(product, [])
    }
  }

  return (
    <div className="flex flex-col h-full bg-[#f4f6f9]">
      {/* Search */}
      <div className="px-4 pt-3">
        <input
          value={search}
          onChange={e => setSearch(e.target.value)}
          placeholder="Buscar producto..."
          className="w-full bg-white border border-slate-200 rounded-[10px] px-4 py-2.5 text-[14px] text-slate-900 placeholder-slate-400 outline-none focus:border-blue-400"
        />
      </div>

      {/* Category tabs */}
      <div className="flex gap-2 px-4 py-3 overflow-x-auto flex-shrink-0 scrollbar-none">
        <button
          onClick={() => setActiveCat(null)}
          className={`px-4 py-2 rounded-full text-[13px] font-semibold whitespace-nowrap flex-shrink-0 border ${
            activeCat === null
              ? 'bg-blue-600 border-blue-600 text-white'
              : 'bg-white border-slate-200 text-slate-600 hover:border-blue-300'
          }`}
        >
          Todas
        </button>
        {categories.map(cat => (
          <button
            key={cat.id}
            onClick={() => setActiveCat(cat.id)}
            className={`px-4 py-2 rounded-full text-[13px] font-semibold whitespace-nowrap flex-shrink-0 border ${
              activeCat === cat.id
                ? 'bg-blue-600 border-blue-600 text-white'
                : 'bg-white border-slate-200 text-slate-600 hover:border-blue-300'
            }`}
          >
            {cat.name}
          </button>
        ))}
      </div>

      {/* Products grid */}
      <div className="flex-1 overflow-y-auto px-4 pb-4 grid gap-2.5" style={{ gridTemplateColumns: 'repeat(auto-fill, minmax(130px, 1fr))', alignContent: 'start' }}>
        {visible.map(product => (
          <button
            key={product.id}
            onClick={() => handleProductClick(product)}
            disabled={isPending}
            className={`bg-white border border-slate-200 rounded-[12px] p-3 text-left hover:shadow-md hover:-translate-y-0.5 transition-all ${!product.is_available ? 'opacity-40' : ''}`}
          >
            <div className="text-[14px] font-semibold text-slate-900 leading-snug mb-1.5">{product.name}</div>
            <div className="text-[15px] font-extrabold text-blue-600">{product.price.toFixed(2)} €</div>
            {!product.is_available && (
              <div className="mt-1 text-[10px] font-bold bg-red-50 text-red-600 border border-red-100 px-1.5 py-0.5 rounded uppercase">
                No disponible
              </div>
            )}
            {product.modifierGroups.length > 0 && product.is_available && (
              <div className="mt-1 text-[10px] font-bold bg-amber-50 text-amber-700 border border-amber-100 px-1.5 py-0.5 rounded uppercase">
                + opciones
              </div>
            )}
          </button>
        ))}
      </div>

      {/* Modifier modal */}
      {modalProduct && (
        <ModifierModal
          product={modalProduct}
          onClose={() => setModalProduct(null)}
          onAdd={modifiers => handleAdd(modalProduct, modifiers)}
        />
      )}
    </div>
  )
}
```

- [ ] **Step 4: Crear `components/tpv/OrderView.tsx`**

```tsx
'use client'

import { useState } from 'react'
import { useRouter } from 'next/navigation'
import type { OrderWithItems, OrderItem, Category, ProductWithModifiers } from '@/app/actions/tpv'
import ProductsPanel from './ProductsPanel'
import OrderPanel from './OrderPanel'

interface Props {
  order: OrderWithItems
  categories: Category[]
  products: ProductWithModifiers[]
}

export default function OrderView({ order, categories, products }: Props) {
  const router = useRouter()
  const [items, setItems] = useState<OrderItem[]>(order.items)
  const [error, setError] = useState<string | null>(null)

  return (
    <div className="h-screen flex flex-col overflow-hidden bg-[#f4f6f9]">
      {/* Nav */}
      <nav className="bg-white border-b border-slate-200 px-4 h-[50px] flex items-center gap-3 flex-shrink-0 shadow-sm">
        <button
          onClick={() => router.push('/tpv')}
          className="bg-slate-100 border border-slate-200 text-slate-600 text-[13px] font-medium px-3 py-1.5 rounded-md"
        >
          ← Mapa
        </button>
        <span className="text-[13px] text-slate-400">
          TPV › <strong className="text-slate-800">{order.table.name}</strong>
        </span>
      </nav>

      {error && (
        <div className="bg-red-50 border-b border-red-100 px-4 py-2 text-red-600 text-[13px] flex-shrink-0">
          {error}
          <button onClick={() => setError(null)} className="ml-2 font-bold">✕</button>
        </div>
      )}

      {/* Split layout */}
      <div className="flex flex-1 overflow-hidden">
        <div className="flex flex-col overflow-hidden" style={{ flex: '0 0 60%' }}>
          <ProductsPanel
            orderId={order.id}
            categories={categories}
            products={products}
            onItemAdded={item => setItems(prev => [...prev, item])}
            onError={setError}
          />
        </div>
        <div className="flex flex-col overflow-hidden" style={{ flex: '0 0 40%' }}>
          <OrderPanel
            order={order}
            items={items}
            onItemsChange={setItems}
            onError={setError}
          />
        </div>
      </div>
    </div>
  )
}
```

- [ ] **Step 5: Verificar TypeScript**

```powershell
npx tsc --noEmit
```

- [ ] **Step 6: Commit**

```powershell
git add app/tpv/comanda components/tpv/OrderView.tsx components/tpv/ProductsPanel.tsx components/tpv/OrderPanel.tsx
git commit -m "feat: add comanda view with products panel and order panel"
```

---

## Task 4: Modal de modificadores

**Files:**
- Create: `components/tpv/ModifierModal.tsx`

- [ ] **Step 1: Crear `components/tpv/ModifierModal.tsx`**

```tsx
'use client'

import { useState } from 'react'
import type { ProductWithModifiers, SelectedModifier } from '@/app/actions/tpv'

interface Props {
  product: ProductWithModifiers
  onClose: () => void
  onAdd: (modifiers: SelectedModifier[]) => void
}

export default function ModifierModal({ product, onClose, onAdd }: Props) {
  // selections: Map<groupId, Set<optionId>>
  const [selections, setSelections] = useState<Map<string, Set<string>>>(new Map())

  const getSelected = (groupId: string) => selections.get(groupId) ?? new Set<string>()

  const toggle = (groupId: string, optionId: string, maxSelections: number) => {
    setSelections(prev => {
      const next = new Map(prev)
      const current = new Set(next.get(groupId) ?? [])
      if (current.has(optionId)) {
        current.delete(optionId)
      } else {
        if (maxSelections === 1) {
          current.clear()
        } else if (current.size >= maxSelections) {
          return prev // limit reached
        }
        current.add(optionId)
      }
      next.set(groupId, current)
      return next
    })
  }

  const isValid = product.modifierGroups
    .filter(g => g.is_required)
    .every(g => {
      const count = getSelected(g.id).size
      return count >= (g.min_selections || 1)
    })

  const computedTotal = product.price + product.modifierGroups.flatMap(g =>
    [...getSelected(g.id)].map(optId => {
      const opt = g.options.find(o => o.id === optId)
      return opt?.price_adjustment ?? 0
    })
  ).reduce((s, v) => s + v, 0)

  const buildModifiers = (): SelectedModifier[] =>
    product.modifierGroups.flatMap(g =>
      [...getSelected(g.id)].map(optId => {
        const opt = g.options.find(o => o.id === optId)!
        return { option_id: optId, name: opt.name, price_adjustment: opt.price_adjustment }
      })
    )

  return (
    <div
      className="fixed inset-0 bg-black/40 flex items-center justify-center z-50 p-4"
      onClick={e => e.target === e.currentTarget && onClose()}
    >
      <div className="bg-white rounded-[14px] w-full max-w-md max-h-[85vh] flex flex-col shadow-xl">
        {/* Header */}
        <div className="px-5 py-4 border-b border-slate-100 flex-shrink-0">
          <div className="text-[17px] font-bold text-slate-900">{product.name}</div>
          <div className="text-[12px] text-slate-400 mt-0.5">Selecciona las opciones</div>
        </div>

        {/* Groups */}
        <div className="flex-1 overflow-y-auto">
          {product.modifierGroups.map(group => {
            const selected = getSelected(group.id)
            const isRadio = group.max_selections === 1

            return (
              <div key={group.id} className="px-5 py-4 border-b border-slate-50">
                <div className="flex items-center justify-between mb-1">
                  <span className="text-[13px] font-bold text-slate-700">{group.name}</span>
                  <span className={`text-[10px] font-bold px-2 py-0.5 rounded uppercase tracking-wide border ${
                    group.is_required
                      ? 'bg-red-50 text-red-700 border-red-200'
                      : 'bg-green-50 text-green-700 border-green-200'
                  }`}>
                    {group.is_required
                      ? `Obligatorio · ${group.min_selections || 1} opción`
                      : `Opcional · hasta ${group.max_selections}`}
                  </span>
                </div>

                <div className="space-y-1.5 mt-3">
                  {group.options.map(option => {
                    const isSelected = selected.has(option.id)
                    return (
                      <button
                        key={option.id}
                        onClick={() => toggle(group.id, option.id, group.max_selections)}
                        className={`w-full flex items-center justify-between px-3 py-2.5 rounded-[8px] border text-left transition-colors ${
                          isSelected
                            ? 'border-blue-500 bg-blue-50'
                            : 'border-slate-200 bg-white hover:border-blue-300 hover:bg-slate-50'
                        }`}
                      >
                        <div className="flex items-center gap-2.5">
                          {/* Control: circle for radio, square for checkbox */}
                          <div className={`flex-shrink-0 flex items-center justify-center ${
                            isRadio ? 'w-4 h-4 rounded-full border-[1.5px]' : 'w-4 h-4 rounded border-[1.5px]'
                          } ${isSelected ? 'border-blue-500 bg-blue-500' : 'border-slate-300'}`}>
                            {isSelected && (
                              isRadio
                                ? <div className="w-1.5 h-1.5 bg-white rounded-full" />
                                : <span className="text-white text-[9px] font-bold leading-none">✓</span>
                            )}
                          </div>
                          <span className="text-[13px] text-slate-800">{option.name}</span>
                        </div>
                        <span className={`text-[12px] font-semibold ${option.price_adjustment > 0 ? 'text-blue-600' : 'text-slate-400'}`}>
                          {option.price_adjustment > 0 ? `+${option.price_adjustment.toFixed(2)} €` : '—'}
                        </span>
                      </button>
                    )
                  })}
                </div>
              </div>
            )
          })}
        </div>

        {/* Footer */}
        <div className="px-5 py-4 border-t border-slate-100 flex-shrink-0">
          <div className="flex items-center justify-between mb-3">
            <span className="text-[12px] text-slate-400 uppercase tracking-wide font-semibold">Total</span>
            <span className="text-[22px] font-extrabold text-slate-900">{computedTotal.toFixed(2)} €</span>
          </div>
          <button
            onClick={() => isValid && onAdd(buildModifiers())}
            disabled={!isValid}
            className="w-full bg-blue-600 hover:bg-blue-700 text-white font-bold text-[14px] py-3.5 rounded-[8px] disabled:opacity-40 disabled:cursor-not-allowed"
          >
            Añadir a la comanda
          </button>
        </div>
      </div>
    </div>
  )
}
```

- [ ] **Step 2: Verificar TypeScript**

```powershell
npx tsc --noEmit
```

- [ ] **Step 3: Commit**

```powershell
git add components/tpv/ModifierModal.tsx
git commit -m "feat: add modifier modal"
```

---

## Task 5: Pantalla de cobro

**Files:**
- Create: `app/tpv/cobro/[orderId]/page.tsx`
- Create: `components/tpv/PaymentForm.tsx`

- [ ] **Step 1: Crear `app/tpv/cobro/[orderId]/page.tsx`**

```tsx
import { redirect } from 'next/navigation'
import { getOrderWithItems } from '@/app/actions/tpv'
import PaymentForm from '@/components/tpv/PaymentForm'

interface Props {
  params: Promise<{ orderId: string }>
}

export default async function CobroPage({ params }: Props) {
  const { orderId } = await params
  const order = await getOrderWithItems(orderId)

  if (!order || order.status !== 'open') redirect('/tpv')

  return (
    <div className="min-h-screen bg-[#f4f6f9] flex flex-col">
      <nav className="bg-white border-b border-slate-200 px-5 h-[52px] flex items-center gap-3 shadow-sm flex-shrink-0">
        <a href={`/tpv/comanda/${orderId}`} className="bg-slate-100 border border-slate-200 text-slate-600 text-[13px] font-medium px-3 py-1.5 rounded-md hover:bg-slate-200">
          ← Comanda
        </a>
        <span className="text-[13px] text-slate-400">
          TPV › {order.table.name} › <strong className="text-slate-800">Cobro</strong>
        </span>
      </nav>
      <div className="flex-1 flex items-start justify-center p-6">
        <PaymentForm order={order} />
      </div>
    </div>
  )
}
```

- [ ] **Step 2: Crear `components/tpv/PaymentForm.tsx`**

```tsx
'use client'

import { useState, useTransition } from 'react'
import { useRouter } from 'next/navigation'
import type { OrderWithItems, PaymentMethod } from '@/app/actions/tpv'
import { processPayment } from '@/app/actions/tpv'

interface Props {
  order: OrderWithItems
}

export default function PaymentForm({ order }: Props) {
  const router = useRouter()
  const [method, setMethod] = useState<PaymentMethod>('cash')
  const [cashInput, setCashInput] = useState('')
  const [cardInput, setCardInput] = useState('')
  const [error, setError] = useState<string | null>(null)
  const [isPending, startTransition] = useTransition()

  const total = order.total
  const cashVal = parseFloat(cashInput.replace(',', '.')) || 0
  const cardVal = parseFloat(cardInput.replace(',', '.')) || 0
  const change = method === 'cash' && cashVal >= total ? cashVal - total : 0

  const isValid = (() => {
    if (method === 'cash') return cashVal >= total
    if (method === 'card' || method === 'bizum') return true
    if (method === 'mixed') return Math.abs(cashVal + cardVal - total) < 0.01
    return false
  })()

  const handleConfirm = () => {
    setError(null)
    startTransition(async () => {
      let params
      if (method === 'cash') {
        params = { method: 'cash' as const, cashAmount: cashVal, changeGiven: change }
      } else if (method === 'card') {
        params = { method: 'card' as const, amount: total }
      } else if (method === 'bizum') {
        params = { method: 'bizum' as const, amount: total }
      } else {
        params = { method: 'mixed' as const, cashAmount: cashVal, cardAmount: cardVal }
      }

      const res = await processPayment(order.id, params)
      if ('error' in res) { setError(res.error); return }
      router.push('/tpv')
    })
  }

  const METHODS: { key: PaymentMethod; label: string }[] = [
    { key: 'cash', label: 'Efectivo' },
    { key: 'card', label: 'Tarjeta' },
    { key: 'bizum', label: 'Bizum' },
    { key: 'mixed', label: 'Mixto' },
  ]

  return (
    <div className="bg-white rounded-[12px] border border-slate-200 shadow-sm w-full max-w-md overflow-hidden">
      {/* Header */}
      <div className="px-5 py-4 border-b border-slate-100">
        <div className="text-[16px] font-bold text-slate-900">Cobro — {order.table.name}</div>
        <div className="text-[32px] font-black text-slate-900 mt-1">{total.toFixed(2)} €</div>
      </div>

      {/* Summary */}
      <div className="px-5 py-3 border-b border-slate-100 space-y-1">
        {order.items.map(item => (
          <div key={item.id} className="flex justify-between text-[13px] text-slate-500">
            <span>{item.product_name} ×{item.quantity}</span>
            <span>{item.total_price.toFixed(2)} €</span>
          </div>
        ))}
        <div className="flex justify-between text-[14px] font-bold text-slate-900 pt-2 mt-1 border-t border-slate-100">
          <span>Total</span><span>{total.toFixed(2)} €</span>
        </div>
      </div>

      {/* Payment method */}
      <div className="px-5 py-4">
        <p className="text-[11px] font-bold text-slate-400 uppercase tracking-widest mb-2.5">Método de pago</p>
        <div className="grid grid-cols-2 gap-1.5 mb-4">
          {METHODS.map(m => (
            <button
              key={m.key}
              onClick={() => setMethod(m.key)}
              className={`py-3 rounded-[8px] border text-[14px] font-semibold transition-all ${
                method === m.key
                  ? 'border-blue-600 bg-blue-50 text-blue-700'
                  : 'border-slate-200 bg-slate-50 text-slate-600 hover:border-blue-300'
              }`}
            >
              {m.label}
            </button>
          ))}
        </div>

        {/* Cash */}
        {method === 'cash' && (
          <div className="bg-slate-50 border border-slate-200 rounded-[8px] p-3 mb-4 space-y-2">
            <div className="flex items-center justify-between">
              <span className="text-[12px] text-slate-500">Total</span>
              <span className="text-[15px] font-bold text-slate-900">{total.toFixed(2)} €</span>
            </div>
            <div className="flex items-center justify-between border-t border-slate-200 pt-2">
              <span className="text-[12px] text-slate-500">Entrega cliente</span>
              <input
                value={cashInput}
                onChange={e => setCashInput(e.target.value)}
                placeholder="0,00"
                className="border border-slate-300 rounded-[6px] px-2.5 py-1.5 text-[15px] font-bold w-24 text-right text-slate-900 bg-white focus:border-blue-400 outline-none"
              />
            </div>
            {change > 0 && (
              <div className="flex items-center justify-between border-t border-slate-200 pt-2">
                <span className="text-[12px] text-slate-500">Cambio</span>
                <span className="text-[18px] font-extrabold text-green-700">{change.toFixed(2)} €</span>
              </div>
            )}
          </div>
        )}

        {/* Mixed */}
        {method === 'mixed' && (
          <div className="bg-slate-50 border border-slate-200 rounded-[8px] p-3 mb-4 space-y-2">
            <div className="flex items-center justify-between">
              <span className="text-[12px] text-slate-500">Efectivo</span>
              <input
                value={cashInput}
                onChange={e => setCashInput(e.target.value)}
                placeholder="0,00"
                className="border border-slate-300 rounded-[6px] px-2.5 py-1.5 text-[15px] font-bold w-24 text-right text-slate-900 bg-white focus:border-blue-400 outline-none"
              />
            </div>
            <div className="flex items-center justify-between border-t border-slate-200 pt-2">
              <span className="text-[12px] text-slate-500">Tarjeta</span>
              <input
                value={cardInput}
                onChange={e => setCardInput(e.target.value)}
                placeholder="0,00"
                className="border border-slate-300 rounded-[6px] px-2.5 py-1.5 text-[15px] font-bold w-24 text-right text-slate-900 bg-white focus:border-blue-400 outline-none"
              />
            </div>
            {Math.abs(cashVal + cardVal - total) > 0.01 && (cashVal + cardVal) > 0 && (
              <p className="text-[11px] text-red-500 pt-1">Los importes no suman {total.toFixed(2)} €</p>
            )}
          </div>
        )}

        {error && <p className="text-red-600 text-[13px] mb-3">{error}</p>}

        <button
          onClick={handleConfirm}
          disabled={!isValid || isPending}
          className="w-full bg-green-700 hover:bg-green-800 text-white font-bold text-[14px] py-4 rounded-[8px] disabled:opacity-40 disabled:cursor-not-allowed"
        >
          {isPending ? 'Procesando...' : 'Confirmar cobro'}
        </button>
      </div>
    </div>
  )
}
```

- [ ] **Step 3: Verificar TypeScript**

```powershell
npx tsc --noEmit
```

- [ ] **Step 4: Commit**

```powershell
git add app/tpv/cobro components/tpv/PaymentForm.tsx
git commit -m "feat: add payment screen"
```

---

## Task 6: Dashboard link + build final

**Files:**
- Modify: `app/dashboard/page.tsx`

- [ ] **Step 1: Añadir enlace al TPV en el dashboard**

En `app/dashboard/page.tsx`, añadir el enlace tras el título de bienvenida:

```tsx
// app/dashboard/page.tsx
import { redirect } from 'next/navigation'
import { createClient } from '@/lib/supabase/server'
import LogoutButton from './LogoutButton'
import Link from 'next/link'

export default async function DashboardPage() {
  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()

  if (!user) {
    redirect('/login')
  }

  return (
    <div className="min-h-screen bg-gray-50 flex items-center justify-center px-4">
      <div className="w-full max-w-md bg-white rounded-2xl shadow-md p-8 text-center">
        <p className="text-sm text-gray-500 mb-1">RP Restaurantes</p>
        <h1 className="text-2xl font-bold text-gray-900 mb-2">Panel de control</h1>
        <p className="text-gray-900 mb-6">
          Bienvenido,{' '}
          <span className="font-medium text-gray-900">{user.email}</span>
        </p>
        <Link
          href="/tpv"
          className="block w-full bg-blue-600 hover:bg-blue-700 text-white font-bold py-3 rounded-lg mb-3 transition-colors"
        >
          Ir al TPV
        </Link>
        <LogoutButton />
      </div>
    </div>
  )
}
```

- [ ] **Step 2: Verificar TypeScript**

```powershell
npx tsc --noEmit
```

- [ ] **Step 3: Build de producción**

```powershell
npm run build
```

Resultado esperado:
```
✓ Compiled successfully
Route (app)
├ ƒ /tpv
├ ƒ /tpv/comanda/[orderId]
├ ƒ /tpv/cobro/[orderId]
├ ƒ /dashboard
...
```

- [ ] **Step 4: Commit final**

```powershell
git add app/dashboard/page.tsx
git commit -m "feat: complete TPV - table map, order view, payment screen"
```

---

## Self-Review

### Spec coverage

| Requisito | Tarea |
|-----------|-------|
| Mapa de mesas agrupadas por zonas | Task 2 |
| Tarjeta con nombre, capacidad, estado, tiempo, total | Task 2 (TableCard) |
| Realtime en mapa | Task 2 (TableMap useEffect) |
| Botón actualizar | Task 2 (TableMap handleRefresh) |
| Clic mesa libre → crear comanda | Task 2 (createOrder) |
| Clic mesa ocupada → abrir comanda | Task 2 (getOpenOrder) |
| Vista split 60/40 | Task 3 (OrderView) |
| Tabs categorías + buscador | Task 3 (ProductsPanel) |
| Productos agotados con badge | Task 3 (ProductsPanel) |
| Modal de modificadores | Task 4 (ModifierModal) |
| Grupos obligatorios/opcionales | Task 4 |
| Snapshot producto en order_items | Task 1 (addOrderItem) |
| Botón Cobrar fijo | Task 3 (OrderPanel — sticky bottom) |
| Cancelar comanda | Task 3 (OrderPanel + cancelOrder) |
| Pantalla de cobro 4 métodos | Task 5 (PaymentForm) |
| Cambio automático en efectivo | Task 5 |
| Validación mixto suma total | Task 5 |
| Crear ticket + payment | Task 1 (processPayment) |
| get_next_ticket_number() | Task 1 |
| VERI*FACTU campos a null | Task 1 (no se insertan) |
| Dashboard link | Task 6 |
| TypeScript sin any | Todas |
